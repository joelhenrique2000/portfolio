import React from 'react'

export default class Contact extends React.Component {
    render() {
        return(
            <section id={'Contact'}>
                Contact
            </section>
        )
    }
}